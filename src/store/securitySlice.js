import { createSlice } from "@reduxjs/toolkit";

const securitySlice = createSlice({
  name: "security",

  initialState: {},

  reducers: {},
});

export const securitySliceAction = securitySlice.actions;

export default securitySlice;
